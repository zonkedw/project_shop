# 🚀 Quick Start Guide - FitPilot

Быстрое руководство для запуска проекта за 5 минут.

## Что уже сделано?

✅ **Backend инфраструктура** (Node.js + Express)  
✅ **Схема базы данных** (PostgreSQL, 20+ таблиц)  
✅ **Модуль аутентификации** (регистрация/вход с JWT)  
✅ **Полная документация** (API, БД, установка)  

---

## Минимальный запуск

### 1. Установить зависимости

```bash
# PostgreSQL 14+
# Node.js 16+
# Git
```

### 2. Клонировать и настроить

```bash
git clone https://github.com/zonkedw/project_shop.git
cd project_shop-main
git checkout kursov_fitness
```

### 3. Создать БД

```bash
# Создать базу данных
createdb fitpilot_db

# Применить схему
psql -d fitpilot_db -f database/schema.sql
```

### 4. Настроить backend

```bash
cd backend
npm install

# Создать .env
copy .env.example .env

# Отредактировать .env:
# DB_PASSWORD=ваш_пароль
# JWT_SECRET=случайная_строка_32+_символа
```

### 5. Запустить

```bash
npm run dev
```

Откройте: http://localhost:3000

---

## Тест API

### Регистрация пользователя

```bash
curl -X POST http://localhost:3000/api/auth/register \
  -H "Content-Type: application/json" \
  -d '{
    "email": "test@example.com",
    "password": "Test123456",
    "username": "testuser"
  }'
```

**Ответ:**
```json
{
  "message": "Регистрация успешна",
  "token": "eyJhbGciOiJIUzI1NiIsInR5cCI6...",
  "user": {
    "user_id": "uuid",
    "email": "test@example.com",
    "username": "testuser"
  }
}
```

### Авторизация

```bash
curl -X POST http://localhost:3000/api/auth/login \
  -H "Content-Type: application/json" \
  -d '{
    "email": "test@example.com",
    "password": "Test123456"
  }'
```

---

## Структура файлов

```
project_shop-main/
├── backend/           ← Node.js сервер
│   ├── config/       ← Подключение к БД
│   ├── routes/       ← API роуты
│   ├── middleware/   ← Auth, validation
│   └── server.js     ← Точка входа
├── database/
│   └── schema.sql    ← SQL схема
├── docs/
│   ├── API.md        ← Документация API
│   ├── DATABASE.md   ← Описание БД
│   ├── SETUP.md      ← Установка
│   └── PROGRESS.md   ← Журнал прогресса
└── README.md         ← Главный README
```

---

## Что делать дальше?

### Следующие задачи:
1. **Модуль питания** - CRUD продуктов и приемов пищи
2. **Модуль тренировок** - упражнения и сессии
3. **Frontend Flutter** - мобильное приложение
4. **AI-ассистент** - персональные рекомендации

### Документация:
- [Полная инструкция установки](docs/SETUP.md)
- [API документация](docs/API.md)
- [Схема базы данных](docs/DATABASE.md)
- [Журнал прогресса](docs/PROGRESS.md)

---

## Помощь

**Проблемы с подключением к БД?**
```bash
# Проверить статус PostgreSQL
# Windows: Get-Service postgresql*
# macOS: brew services list
# Linux: sudo systemctl status postgresql
```

**Порт 3000 занят?**
Измените `PORT=3001` в `.env`

**JWT ошибка?**
Убедитесь, что `JWT_SECRET` в `.env` длиннее 32 символов

---

## Контакты

- **Авторы:** Качкалов М.О., Чернышова В.Ю., Шпитонков К.А.
- **Группа:** 22ИС4-2
- **Руководитель:** Федотов И.В.

---

**GitHub:** https://github.com/zonkedw/project_shop/tree/kursov_fitness

**Дата создания:** 09.11.2025
